# Docs only scriv config to capture unreleased fragments for dev branch docs without messing with real changelog

[tool.scriv]
format = md
fragment_directory = changelog.d
output_file = CHANGELOG_docs.md
md_header_level = 2
categories = Added, Changed, Fixed, Removed, Documentation, Maintenance
version = literal: pyproject.toml: tool.poetry.version